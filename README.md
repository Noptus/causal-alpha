# causal-alpha

Reproducible experiments for causal regime-aware alpha discovery with synthetic and real open-data benchmarks.

Primary entrypoint:

```bash
python -m experiments.run --config synthetic_main
```

